##################################################
#  GitHub Continuous Integration via Travis CI:  #
#  https://travis-ci.com                         #
##################################################

dist: trusty

git:
  depth: false

install:
  - npm install -g eclint

script:
  # ==============================================
  # EditorConfig Code Styles Validation via EClint
  # ==============================================
  # https://editorconfig.org
  # https://www.npmjs.com/package/eclint
  - bash ./validate.sh

# EOF #
